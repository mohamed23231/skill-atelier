---
description: Design a technical solution and visualize current vs proposed architecture
argument-hint: [feature or requirement]
---

Use the `architecture-visualizer` skill to design: $ARGUMENTS

Deliver current architecture, proposed architecture, the delta (`ADDED` / `CHANGED` / `REMOVED`), data flow,
sequence flow, database impact, ADRs, failure modes, and a phased implementation plan whose tasks reference real
file paths. Stop at the approval gate — do not implement until the user approves.
