---
description: Model and visualize an existing or proposed system architecture
argument-hint: [system, service, or module]
---

Use the `architecture-visualizer` skill to produce a current-state architecture model of: $ARGUMENTS

Ground every component in real files from this repository (cite paths in `details.files`), mark anything you
could not verify as `INFERRED` or `ASSUMED`, and finish with the interactive HTML plus the 14-point gate report.
