---
description: Build an interactive architecture visualization for a feature, system, or change
argument-hint: [what to visualize]
---

Use the `architecture-visualizer` skill to model and visualize: $ARGUMENTS

Follow the skill's workflow exactly — UNDERSTAND → MODEL → CHOOSE DIAGRAMS → VISUALIZE → VALIDATE → EXPLAIN.
Inspect this repository before modelling, badge every node `VERIFIED` / `INFERRED` / `ASSUMED` / `UNKNOWN`,
compile with `bin/arch-viz.js build`, and report the quality gate result. Do not modify application code.
